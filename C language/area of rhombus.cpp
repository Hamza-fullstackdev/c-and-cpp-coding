#include<stdio.h>
#include<conio.h>
int main()
{
	int p,q;
		printf("Enter the one side diagonal=");
		scanf("%d",&p);
		printf("Enter the other side=");
		scanf("%d",&q);
    float area=(p*q/2);
    printf("Enter the area of rhombus=%f",area);
}
