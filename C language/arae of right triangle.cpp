#include<stdio.h>
#include<conio.h>
int main()
{
	int l,m;
	
		printf("Enter the length of a triangle=");
		scanf("%d",&l);
		printf("Enter the breadth of a triangle=");
		scanf("%d",&m);
	float area;
	area=(l*m)/2;
		printf("The arae of triangle is=%f",area);
}

